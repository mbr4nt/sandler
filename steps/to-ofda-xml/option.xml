<Option Sequence="${context.sequence}">
    <Code>${context.code}</Code>
    <Range>
    </Range>
    <Description Language="en-US">${context.description}</Description>
    ${materialApplication(context)}
    <Features Sequence="1">
        ${subFeatures(context)}
    </Features>
    ${optionPrices(context)}
</Option>